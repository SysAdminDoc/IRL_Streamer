<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
            Debug-only deep link for the audit state selector.

            The release build has no handler for it: MainActivity.handleDebugIntent
            returns immediately when ENABLE_DEBUG_STATE_SELECTOR is false. Shipping
            the filter in release advertised a browsable scheme that did nothing,
            and squatted on the scheme the settings import flow will define.
        -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="irlstreamer" />
            </intent-filter>
        </activity>
    </application>
</manifest>
